{
  "name": "section-scroll-snap",
  "version": "1.0.0",
  "scripts": {
    "build": "postcss src/styles.css -o src/compiled.css",
    "postbuild": "echo $(awk -f css-comments.awk src/compiled.css) > src/compiled.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Fecke (@leptoquark1)",
  "license": "Apache-2.0",
  "dev-dependencies": {
    "autoprefixer": "^10.1.0",
    "postcss": "^8.2.2",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.0.2"
  }
}
